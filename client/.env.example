# Optional. If unset, the API base is "/api" which the Vite dev server proxies to localhost:5000.
# Set this to your deployed backend URL (e.g. https://taskflow-api.onrender.com/api) when building for production.
VITE_API_URL=